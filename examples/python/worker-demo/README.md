# Python Worker Demo

Planned placeholder for the future Python SDK demo worker.

Rule for implementation: once `sdks/python/<sdk-name>/` exists, this directory must become a standalone buildable/runnable demo project under `examples/python/worker-demo/` and must not depend on the server Dockerfile.
