# Development/single-node install. Stores SQLite on a PVC.
server:
  replicas: 1
  storage:
    mode: sqlite
    type: sqlite
    sqlite:
      path: /data/tikeo.db
      mode: rwc
    persistence:
      enabled: true
      size: 5Gi
web:
  replicas: 1
