# Tikeo 示例中文入口 🚀

`examples/` 目录提供 Java、Rust、Go、Python、Node.js Worker demo。所有 demo 都应对齐同一组能力：结构化 processor、插件 processor、脚本 runner、任务日志、SDK 诊断日志和管理 API 示例。

启动前先运行：

```bash
./scripts/dev.sh
./scripts/dev-seed.sh
```

然后进入对应语言目录按 README 启动 demo。
