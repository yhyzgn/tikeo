global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/tikee-recording-rules.yml

scrape_configs:
  - job_name: tikee
    metrics_path: /metrics
    static_configs:
      - targets:
          - tikee:9090
