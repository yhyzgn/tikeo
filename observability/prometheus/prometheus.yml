global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/tikeo-recording-rules.yml

scrape_configs:
  - job_name: tikeo
    metrics_path: /metrics
    static_configs:
      - targets:
          - tikeo:9090
