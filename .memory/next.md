# 下一步任务

执行 `.prompt/007-web-ui-foundation.md`：

1. 初始化 `./web` Bun 工程。
2. 使用 React + TypeScript + Vite + Ant Design。
3. 建立基础布局、路由、主题和暗色模式预留。
4. 实现 Dashboard、Job 列表、创建 Job、手动触发、实例列表/详情骨架。
5. API client 以 `/api-docs/openapi.json` 为契约，保持 `{code,message,data}` 解析规范。
6. 验证 Web：`bun install`、`bun run lint`、`bun run typecheck`、`bun test`、`bun run build`。
7. 同时保持后端 cargo 全量验证与 Java Maven 验证通过。
8. 更新设计路线图、`.memory` 和后续 `.prompt`。
9. 提交并推送。
