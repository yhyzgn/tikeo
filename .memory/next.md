# 下一步任务

执行 `.prompt/004-storage-and-scheduler.md`：

1. 新增 `crates/scheduler-storage`。
2. 引入 SeaORM / migration 工具链。
3. 定义最小 namespace/app/job/job_instance 数据模型。
4. 支持 SQLite dev database 配置。
5. 将 Jobs API 从 placeholder 接到真实 repository。
6. 保持 `{code,message,data}` 响应规范。
7. 保持 HTTP/OpenAPI 与 Worker Tunnel 验证通过。
8. 更新设计路线图、`.memory` 和后续 `.prompt`。
9. 提交并推送。
